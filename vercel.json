{
    "builds": [{
        "src": "personalwebapp/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "personalwebapp/wsgi.py"
        }
    ]
}